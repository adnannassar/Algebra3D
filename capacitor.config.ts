import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.adnannassar.app',
  appName: 'Algebra3D',
  webDir: 'dist/Algebra3D',
  bundledWebRuntime: false
};

export default config;
